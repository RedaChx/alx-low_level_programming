#include <stdio.h>
#include <stdlib>

/**
 * main - prints its name, followed by a new line
 * @argc: argument count
 * @argv: arguments
 *
 * Return: 0
 */
int main(int argc, char **argv)
{
	printf("%\n", argv[0]);
	Return(0);
}
